import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.spatialneglect.app',
  appName: 'תרגול סריקה מרחבית',
  webDir: 'dist'
};

export default config;
